version: '2.1'

volumes:

  ssh-data:

services:

  rsnapshot:
    build: ./rsnapshot
    privileged: true
    volumes:
      - 'ssh-data:/root/.ssh'

  ssh:
    image: klutchell/resin-ssh
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'

