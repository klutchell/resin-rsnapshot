version: '2.1'

volumes:

  ssh-data:

services:

  rsnapshot:
    build: ./rsnapshot
    # privileged: true
    # devices:
    #   - "/dev/sda:/dev/sda"
    volumes:
      - 'ssh-data:/root/.ssh'

  ssh:
    image: klutchell/resin-ssh
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'

