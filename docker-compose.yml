version: '2.1'

volumes:

  ssh-data:

services:

  rsnapshot:
    build: ./rsnapshot
    # privileged: true
    cap_add:
      - SYS_ADMIN
    volumes:
      - 'ssh-data:/root/.ssh'

  ssh:
    image: klutchell/resin-ssh
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'

